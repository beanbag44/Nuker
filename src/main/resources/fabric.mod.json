{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",
  "name": "${mod_name}",
  "description": "Epic Nuker",
  "authors": ["Beanbag", "PoTheMagicDragon"],
  "contact": { "sources": "https://github.com/beanbag44/Nuker" },
  "license": "MIT",
  "icon": "assets/icon.png",
  "environment": "client",
  "entrypoints": {
    "main": [ "mc.merge.Loader"],
    "meteor": [ "mc.merge.external.meteor.MeteorLoader"]
  },
  "mixins": [ "merge.mixins.json" ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric-language-kotlin": ">=${kotlin_loader_version}",
    "minecraft": "${minecraft_version}"
  }
}
