package mc.merge.render

import net.minecraft.util.math.Box
import java.awt.Color

class Renderer3D : IRenderer3D {
    override fun boxLines(box: Box, color: Color) {
    }

    override fun boxSides(box: Box, color: Color) {
    }
}